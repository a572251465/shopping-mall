server:
  port: 8060

spring:
  application:
    name: mall-gateway

  cloud:
    nacos:
      discovery:
        server-addr: 192.168.56.10:8848

    gateway:
      routes:
        - id: gateway_test
          uri: http://www.baidu.com
          predicates:
            - Query=name,lihh